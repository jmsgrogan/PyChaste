package:
  name: 4suite-xml
  version: "1.0.2"

source:
  fn: 4Suite-XML-1.0.2.tar.gz
  url: https://pypi.python.org/packages/0e/ae/3c5b4fffb12be7c3a80c99475853349e1cf8477f99051921ea06fbf5e3b9/4Suite-XML-1.0.2.tar.gz
  md5: 3ca3db95cb0263ad80beba034d1ff6ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amara = amara:main
    #
    # Would create an entry point called amara that calls amara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.xml3k.org/Amara2
  license: Apache Software License
  summary: 'Library for XML processing in Python'
  license_family: Apache

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
